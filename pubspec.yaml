name: event_card_widget
description: A reusable Flutter EventCard widget for displaying event information. CBSD Project 2026.
version: 1.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cached_network_image: ^3.3.1
  intl: ^0.19.0
